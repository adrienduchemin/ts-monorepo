{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "lib"
  },
  // we should also excluse all of the ones present in ./tsconfig.json if no include is specified
  "exclude": ["**/*.test.ts", "**/*.spec.ts"] // excluse tests for generation
}
