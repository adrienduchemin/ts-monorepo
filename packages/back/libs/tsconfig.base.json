{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // because we're building libs to be used by we don't know who yet
    "module": "CommonJS",
    "target": "ES5",

    // useful for debuging
    "declaration": true,
    "sourceMap": true,
    "declarationMap": true
  }
}
